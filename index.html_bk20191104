<!DOCTYPE html>
<html>

<head>
	<meta charset="utf-8">
	<title>もぐらたたきゲーム</title>
	<style media="screen">
		#game {
			cursor: url(./images/hummer.cur) 40 40, auto;
		}

		h1 {
			text-align: center;
			margin: auto 0px;
			background: rgba(140, 248, 155, 0.7);
		}

		h1>img {
			width: 320px;
		}

		body {
			margin: 0px auto;
			background-image: url(./images/草-noitem.png);
			background-repeat: no-repeat;
			background-attachment: fixed;
			background-position: center;
			background-size: cover;
		}

		.container {
			width: 520px;
			height: 160px;
			margin: 20px auto;
			text-align: center;
			display: -webkit-flex;
			display: -moz-flex;
			display: -ms-flex;
			display: -o-flex;
			display: flex;
		}

		.container img {
			margin: 20px;
			width: 140px;
		}

		.wrapper {
			margin: 0px auto;
			display: flex;
		}

		img {
			width: 213px;
		}

		.hit-mogura {
			cursor: url(./images/hummer2.cur) 20 20, auto;
		}

		.uwagaki {
			position: absolute;
			top: 0;
			left: 0;
		}

		.relative {
			position: relative;
		}

		.field {
			margin: 40px;
		}
	</style>

</head>

<body id="game">
	<div class="field">
		<h1><img src="images/title.png" alt=""></h1>
		<div class="container">
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
		</div>
		<div class="container">
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
		</div>
		<div class="container">
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
			<div class="relative">
				<img class="ana" src="images/穴.png">
				<img class="mogura uwagaki" src="">
			</div>
		</div>
	</div>

	<script>
		//var test;
		// 全てのもぐらに、クリックイベントを登録する
		const moguras = document.querySelectorAll(".mogura");
		const iteiMogura = "images/モグ1.png";
		const normalMogura = "images/モグ2.png";
		setInterval(deruMogura, 1500); //課題１：1000から1500へ

		
		for (let index = 0; index < moguras.length; index++) {
			const element = moguras[index];
			element.onclick = hitMogura;
		}

		// もぐらが叩かれる
		function hitMogura() {
			var mogura = event.target;
			mogura.src = iteiMogura;
			setInterval(iteiMogura, 3000, mogura);　//課題１：叩かれたモグラ3000秒繰り返す
			setTimeout(kakureruMogura, 700, mogura);　//700ミリ秒後に元に戻る
		}

		// // もぐらが戻る
		// function modoruMogura(mogura){
		// 	mogura.src = "images/モグ2.png"; 
		// }

		// もぐらが隠れる(消える)
		function kakureruMogura(mogura) {
			mogura.src = "";
		}

		function deruMogura() {
			var random = Math.floor(Math.random() * 9);
			var mogura = moguras[random];
			mogura.src = normalMogura;
			// しばらくしたら隠れる
			setTimeout((mogura) => {
				if (mogura.src.indexOf(normalMogura)) {
					kakureruMogura(mogura);
				}
			}, 1500, mogura); //課題１：隠れるまでの時間を600から1500ミリ秒
		}

		

		function allstop(mogura){
			
			// mogura.src = normalMogura;
			alert("終了！");

		};

		setTimeout(allstop,5000);
		
		// setTimeout (() => {
		// 	alert("終了！")
		// },5000);


	</script>
</body>

</html>